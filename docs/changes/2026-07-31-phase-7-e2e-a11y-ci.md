# Change record — Phase 7 Playwright E2E, a11y, responsive, CI

**Date:** 2026-07-31  
**Branch:** `test/e2e-a11y-ci`  
**Pull request:** pending  
**Recovery phase:** 7  
**Status:** COMPLETE

## Summary

Adds Playwright E2E with mock AI route interception (no Replicate cost), axe-core WCAG 2.1 AA smoke on the demo gate, privacy, and try-on compare step, responsive and reduced-motion checks, a dedicated `e2e` CI workflow, and minimal customer-facing UI pages required for the happy path including the **Demostración** mock badge.

## Recovery phase

7 — Playwright, CI, a11y, responsive.

## Scope included

- `playwright.config.ts`, `e2e/**` (happy path, a11y, responsive)
- `@playwright/test`, `@axe-core/playwright` dev dependencies
- `.github/workflows/ci.yml` — `e2e` job (`AI_PROVIDER=mock`, route mocks; no Supabase secrets)
- Minimal UI: home, try-on flow, gate with `next` redirect, brand tokens, AA contrast fixes
- npm scripts: `test:e2e`, `test:e2e:ui`
- Docs: change record, agent run, testing-strategy note

## Scope excluded

- Full booking → admin → inbox → confirm E2E (requires Supabase; deferred)
- E2E against Replicate real (cost)
- Restoring `DATA_STORE=memory` operational API routes (Playwright mocks used instead)
- Moving `quality` job out of `governance.yml` (branch protection preserved)

## Architecture impact

- E2E runs against `next dev` with Playwright network mocks for try-on APIs
- `governance.yml` unchanged (`governance` + `quality` job names preserved)
- CI adds parallel `e2e` workflow on PR/push to `main`

## API impact

None on production routes. Gate form now honours safe `?next=` redirect after demo access.

## Data and migration impact

None.

## Security and privacy impact

- CI uses fixed demo secrets (non-production); no Replicate token
- E2E fixture image is a 10×10 PNG stub (no personal data)
- `next` redirect validated (same-origin path only)

## Testing evidence

| Check | Command | Result |
|------|---------|--------|
| Lint | `npm run lint` | PASS |
| Typecheck | `npm run typecheck` | PASS |
| Unit tests | `npm run test` | PASS (139) |
| E2E | `npm run test:e2e` | PASS (14 — desktop + mobile) |
| Build | `npm run build` | PASS |

## Deployment and rollback

No runtime deploy. Roll back by disabling/quarantining the `e2e` job in `.github/workflows/ci.yml`.

## Documentation updated

- `docs/testing-strategy.md`
- `docs/changes/2026-07-31-phase-7-e2e-a11y-ci.md` (this file)
- `docs/agent-runs/2026-07-31-phase-7.md`

## Remaining risks

- Try-on APIs mocked in E2E — does not exercise Supabase persistence paths
- `next dev` in E2E differs from production `next start` runtime
- Full flow (admin/inbox/confirm) still manual or future phase

## Verification status

- Planner: N/A (scope defined in recovery plan)
- Architect: N/A
- Security: mock-only CI; no secrets committed
- Tests: 14 E2E + 139 unit
- Independent verifier: pending PR CI
