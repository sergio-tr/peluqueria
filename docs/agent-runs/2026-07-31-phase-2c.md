# Agent run — Phase 2C

**Date:** 2026-07-31  
**Branch:** `feature/idempotent-confirmation-expiration`  
**Predecessor:** `fix/booking-transaction-consistency` (PR #7)  
**Phase:** 2C

## Outcome

D-08 confirm idempotency with hash-only tokens, durable idempotency keys for `booking-confirmed:{booking_id}`, proper 404/410/409 responses, and shared expire-due for cron + admin. CONFIRMED bookings excluded from expiration query.

## Files touched

- `supabase/migrations/20260731120000_idempotency_keys.sql`
- `src/domain/notification-idempotency.ts` (+ tests)
- `src/domain/booking-holds.test.ts`
- `src/infrastructure/persistence/repositories/idempotency-keys.ts` (+ tests)
- `src/application/bookings/confirm-booking.ts` (+ tests)
- `src/application/bookings/expire-due.test.ts`
- `netlify.toml`, `netlify/functions/expire-bookings.mts`
- docs

## Blockers

- Live Supabase must apply migration `20260731120000_idempotency_keys.sql`
- Netlify scheduled function hourly cron not verified against production (Draft PR if unconfigured)

## Stack

Merge after PR #7. Order: … → #7 → 2C.

## Operator verify

1. Apply idempotency migration on Supabase
2. Confirm twice with same token → second response 200, single `booking_events` confirm row
3. Configure Netlify scheduled function or verify `@hourly` in `expire-bookings.mts`
