# Agent run — Phase 3A

**Date:** 2026-07-31  
**Branch:** `chore/netlify-preview-bootstrap`  
**Predecessor:** `update/catalog-production-assets` (PR #9)  
**Phase:** 3A — Netlify preview bootstrap

## Outcome

Preview deployment configuration and runbook shipped. Portfolio pattern documented from `portfolio-json` (Netlify-hosted Astro, Git-connected). Netlify CLI not authenticated — Draft PR; operator must complete site link and health verification.

## Base commit

`9c19e57` (tip of `update/catalog-production-assets` at branch creation)

## Portfolio evidence

See `docs/portfolio-netlify-evidence.md`.

Selected candidate: `c:\Users\Sergio\workspaces\portfolio-json`  
Remote: `https://github.com/sergio-tr/portfolio-json.git`  
Signals: README deploy URLs (`sergiotr.netlify.app`), package name `minimalist-portfolio-json`.

## Files changed

- `netlify.toml` — Node 20, remove manual publish dir, functions bundler
- `src/app/api/health/route.ts`, `route.test.ts`
- `src/middleware.ts` — public `/api/health`
- `docs/deployment.md` — preview runbook
- `docs/portfolio-netlify-evidence.md`
- `docs/operator-actions.md` — OP-002, OP-004–OP-007
- `docs/changes/2026-07-31-phase-3a-netlify-preview-bootstrap.md`

## Commands

```bash
node .cursor/scripts/find-portfolio-netlify.mjs   # no sibling netlify.toml
npx netlify status                                 # Not logged in
npm run lint && npm run typecheck && npm test && npm run build
node .cursor/scripts/check-pr-readiness.mjs
```

## Tests

- New: `src/app/api/health/route.test.ts`
- Full suite: see change record after CI/local run

## Blockers

| ID | Blocker | Resolution |
|----|---------|------------|
| OP-002 | Netlify CLI not logged in | Operator runs `netlify login` |
| OP-005 | No linked preview site | Operator `netlify init` + deploy |
| OP-004/OP-006 | Supabase remote not provisioned | Operator creates project + `db push` |
| OP-007 | Health checklist not run against live URL | After first preview deploy |

## Stack dependency

Merge after PR #9 (`update/catalog-production-assets`). Phase 3B depends on preview HTTPS URL for Replicate webhook.

## Remaining risks

- `GET /api/services` health check #4 requires Supabase + seed on preview
- Scheduled function `expire-bookings` unverified until Netlify deploy with `CRON_SECRET`

## PR

pending — Draft until OP-007 complete
