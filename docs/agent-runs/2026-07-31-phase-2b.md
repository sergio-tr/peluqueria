# Agent run — Phase 2B

**Date:** 2026-07-31  
**Branch:** `fix/booking-transaction-consistency`  
**Predecessor:** `security/admin-auth-api-protection` (PR #6)  
**Phase:** 2B

## Outcome

Booking writes use Postgres RPC transactions with `booking_events`. Exclusion constraint on blocking statuses (D-07) is the source of truth for slot occupancy; conflicts map to 409. Reject/decline/expire transitions release slots atomically with status change.

## Files touched

- `supabase/migrations/20260731110000_booking_tx_rpcs.sql`
- `src/domain/booking-slot.ts` (+ tests)
- `src/infrastructure/persistence/postgres-errors.ts` (+ tests)
- `src/infrastructure/persistence/repositories/booking-transactions.ts` (+ tests)
- `src/application/bookings/create-booking-request.ts`
- `src/application/bookings/barber-transition.ts`
- `src/application/bookings/confirm-booking.ts`
- `src/application/bookings/expire-due.ts`
- `src/domain/booking-state.test.ts`
- docs

## Blockers

- Live Supabase must apply migration `20260731110000_booking_tx_rpcs.sql`; GiST double-book not verified against production DB in CI (Draft PR if exclusion unverified on target env).

## Stack

Merge after PR #6 (`security/admin-auth-api-protection`). Order: … → #6 → 2B.

## Operator verify (live DB)

After migration, run two concurrent `POST /api/booking-requests` for the same staff/slot: expect one 201 and one 409 `SLOT_UNAVAILABLE`.
