# Agent run — Phase 9 production DoD smoke checklist

**Date:** 2026-07-31  
**Branch:** `test/production-dod-smoke`  
**Base:** `release/netlify-production` (PR #17)  
**PR:** https://github.com/sergio-tr/peluqueria/pull/18 (Draft)

## Objective

Document Phase 9 production smoke DoD checklist matching recovery plan: Acceso → foto → corte → gen real → solicitud → revisión → propuesta → Demo Inbox → confirm → agenda bloqueada → solape rechazado. Provide evidence template and operator runbook (OP-017). **Do not** declare DoD VERIFIED without live public Replicate evidence.

## Actions taken

1. Created branch `test/production-dod-smoke` from `release/netlify-production`.
2. Added `docs/dod-smoke-checklist.md` with PENDING evidence slots (URL, timestamp, screenshots).
3. Added `scripts/production-dod-smoke/` — evidence template + CLI (`--init`, `--validate`).
4. Added OP-017 to `docs/operator-actions.md`.
5. Updated testing strategy, deployment, implementation status with NOT VERIFIED DoD.
6. Gitignored `smoke-evidence/` for operator screenshots.

## Test results

- `npm run lint`, `typecheck`, `test` (139), `build` — PASS
- `npm run smoke:dod`, `npm run smoke:dod -- --init` — PASS
- `node .cursor/scripts/check-pr-readiness.mjs` — PASS

## Blockers

- No production URL verified in agent environment
- OP-013–OP-016 (Phase 8) must complete before OP-017
- Public Replicate smoke requires operator budget and credentials
- DoD status remains **NOT VERIFIED**

## CI expectation

- `governance` + `quality` + `e2e` unchanged
- Docs + script only; no application code changes
