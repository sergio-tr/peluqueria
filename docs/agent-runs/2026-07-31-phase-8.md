# Agent run — Phase 8 Netlify production runbook

**Date:** 2026-07-31  
**Branch:** `release/netlify-production`  
**Base:** `test/e2e-a11y-ci` (PR #16)  
**PR:** pending (Draft)

## Objective

Production release runbook for Netlify: fail-closed env checklist, cron hourly, webhook prod URL, kill switches, previous-deploy rollback. Reuse portfolio deployment mechanism without copying secrets. Do not execute prod deploy or declare production live.

## Actions taken

1. Created branch `release/netlify-production` from `test/e2e-a11y-ci`.
2. Expanded `docs/deployment.md` with Production release (Phase 8) section.
3. Added operator actions OP-013–OP-016 for production env, deploy, webhook, health.
4. Added `[context.production.environment] APP_ENV=production` to `netlify.toml`.
5. Documented kill switches and ADR-015 / C-08 rollback (no migrate down).

## Test results

- `npm run lint`, `typecheck`, `test` (139), `build` — PASS
- `node .cursor/scripts/check-pr-readiness.mjs` — PASS

## Blockers

- Netlify CLI not installed / not logged in in agent environment
- Operator must complete OP-002, OP-013–OP-016 before first `--prod` publish
- Production not declared live until Phase 9 smoke DoD

## CI expectation

- `governance` + `quality` + `e2e` unchanged
- Docs-only change; `netlify.toml` context addition only
