# Agent run — Phase 2A

**Date:** 2026-07-31  
**Branch:** `security/admin-auth-api-protection`  
**Predecessor:** `feature/operational-persistence` (PR #5)  
**Phase:** 2A

## Outcome

Supabase Auth replaces `ADMIN_DEMO_KEY` for admin APIs and UI. Demo gate is env-configured with signed cookie and IP-hash rate limiting. Middleware enforces admin session on `/admin` (except login) and demo gate on public flows.

## Files touched

- `src/infrastructure/api/require-admin.ts` (+ tests)
- `src/infrastructure/supabase/auth-env.ts`, `browser-client.ts`, `server-auth-client.ts`
- `src/infrastructure/demo-session.ts`, `demo-rate-limit.ts`, `ip-hash.ts` (+ tests)
- `src/middleware.ts`
- `src/app/admin/**`, `src/app/acceso/**`, `src/app/api/demo-access/**`, `src/app/api/auth/logout/**`
- Admin API routes (async `requireAdmin`)
- `supabase/migrations/20260731100000_staff_auth_user_fk.sql`
- `.env.example`, docs

## Blockers

- Operator must create Supabase Auth user and set `staff.auth_user_id` before admin login succeeds (403 until linked).

## Stack

Merge after PR #5 (`feature/operational-persistence`). Order: #1 → #2 → #3 → #4 → #5 → 2A.
