# Agent run — Phase 4

**Date:** 2026-07-31  
**Branch:** `feature/durable-demo-inbox`  
**Predecessor:** `feature/replicate-storage-limits` (PR #12)  
**Phase:** 4

## Outcome

`NotificationPort` + `DemoInboxNotificationAdapter` with durable Postgres inbox messages. Propose creates customer proposal in Demo Inbox; confirm sends final notification once using D-08 idempotency key `booking-confirmed:{booking_id}`. Admin inbox API/UI read from DB.

## Files touched

- `src/domain/notifications/notification-port.ts` (+ tests)
- `src/infrastructure/notifications/demo-inbox-notification-adapter.ts` (+ tests)
- `src/application/bookings/barber-transition.ts` (+ tests)
- `src/application/bookings/confirm-booking.ts` (+ tests)
- docs

## Blockers

- Live Supabase must apply migrations from 1D and 2C before deploy
- Predecessor PR #12 not merged — stack dependency

## Stack

Merge after PR #12. Order: … → #12 → Phase 4.

## Operator verify

1. Apply 1D + 2C migrations on Supabase if not already applied
2. Admin propose → Demo Inbox shows proposal link
3. Client confirm → second inbox message; repeat confirm → 200 without duplicate final message
